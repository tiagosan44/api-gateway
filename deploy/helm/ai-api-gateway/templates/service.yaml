apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-api-gateway.fullname" . }}
  labels:
    {{- include "ai-api-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-api-gateway.selectorLabels" . | nindent 4 }}

